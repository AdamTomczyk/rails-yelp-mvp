<%= simple_form_for  @restaurant do |f| %>

<%= f.input :name %>
<%= f.input :address %>
<%= f.input :category, :collection => %w[french belgian italian chinese japanese] %>
<%= f.input :phone_number %>

  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
